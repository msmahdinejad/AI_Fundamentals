# Phase 3: Adversarial Search Game

This phase implements a two-player Paint Battle environment and an AI agent based on depth-limited minimax with alpha-beta pruning. The evaluation function combines score difference, territory control, mobility, distance pressure, and opponent constraints.

The packaged enemy agents in `src/bin/` are required by `main.py` and are intentionally preserved.
